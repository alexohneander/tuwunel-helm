{{- if .Values.tomlConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tuwunel.fullname" . }}-config
  labels:
    {{- include "tuwunel.labels" . | nindent 4 }}
data:
  {{ .Values.tomlConfig.configName }}: |-
{{ .Values.tomlConfig.data | indent 4 }}
{{- end }}
